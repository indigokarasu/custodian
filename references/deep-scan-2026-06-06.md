# Deep Scan Evidence — 2026-06-06

## Pitfalls Exercised
- **Critical pitfall #4c**: Gateway log tiny after restart — checked errors.log as primary source ✓
- **Critical pitfall #4d**: Shell quoting — used heredoc for all Python via terminal() ✓
- **Critical pitfall #5**: Gateway systemd false negative — systemd=inactive but process running, port 8080 not bound ✓
- **Critical pitfall #6**: NoneType f-string crash — used `or 'N/A'` coercion for nullable fields ✓
- **Deep-scan pitfall**: `last_status=error` with `consecutive_failures=0` means transient — all 16 error jobs confirmed transient ✓
- **Deep-scan pitfall**: Orphan detection — 2 never-ran jobs (forge:skill-audit, ocas-autobio-update) have future next_run_at, correctly NOT flagged as orphaned ✓
- **NEW pitfall**: Skill directory path mismatch — `ocas-elephas` exists at `skills/elephas/ocas-elephas/`, not `skills/ocas-elephas/`. Jobs running fine despite non-standard path.
- **NEW pitfall**: Conformance parsing over-broad — initial parse picked up schedule strings as job names. Refined to colon-separated identifiers only.

## Fix Counts
- Tier 1 fixes applied: 0
- New issues created: 1 (weave:sync-contacts model deprecation)
- Escalations: 1 (tagged `escalation_needed: true`)

## New Patterns Discovered
1. **oc_model_400_deprecated**: `nvidia/nemotron-3-ultra:free is not a valid model ID` — model deprecation error (HTTP 400, distinct from HTTP 404). Added to known_issues.json.

## Script Path Issues
- None — all 12 script-referencing jobs have valid script paths.

## MCP Failures
- None observed in this scan window.

## Memory Trends
- state.db: 13.09 GB (15,479 sessions, 14,278 cron)
- 5,892 cron sessions older than 30 days
- Disk at 94% — pruning recommended, VACUUM not feasible

## Skill Conformance
- 39 declared background tasks, all have matching cron jobs
- 0 missing registrations
- 0 orphaned jobs
