# Deep Scan 2026-06-18 02:00

## Summary

116 jobs, 110 ok, 6 error (all OVH 403 — known escalation). 1 Tier 1 fix applied. System degraded but stable.

## Tier 1 Fix Applied

### `oc_config_empty_section` — Pattern B precursor

- **What**: 9 empty/null top-level keys in `config.yaml` (`command_allowlist`, `credential_pool_strategies`, `fallback_providers`, `honcho`, `hooks`, `personalities`, `providers`, `quick_commands`, `whatsapp`)
- **Action**: Removed all 9 keys. Config re-read confirms 0 empty sections remain.
- **Recurrence**: This fix was **previously applied on 2026-06-17** (deep scan + light scan both applied it). The sections reappeared overnight — likely gateway config.yaml regeneration on restart. This is the 2nd application.
- **Pattern B risk**: If this recurs a 3rd time, trigger Pattern B drilldown (fix doesn't hold across gateway restarts). Root cause hypothesis: gateway startup or config migration regenerates these null keys from a template.
- **Fix direction for next recurrence**: Instead of just removing the keys, investigate what's writing them back. Check gateway config migration code or startup scripts that seed default config. Add a post-startup hook or config validation step that strips null sections.

## Non-Fatal Patterns Confirmed

- **Stale failure counters**: `elephas:ingest` (cf=2, ok, ran 12m ago), `weave-enrichment-health-check` (cf=1, ok, ran 53m ago). Both running normally.
- **Context engine "chronicle" not found**: Config uses `engine: compressor`. Chronicle loads from profile path. Warnings are expected fallback.
- **Gateway health endpoint unreachable**: Port 8080 not bound. Process running (PID 325777, 47h uptime), port 9119 listening. Non-fatal.

## Open Escalations

| Issue | Tier | Affected |
|---|---|---|
| `oc_provider_ovh_403_auth-20260618` | 3 | 6 jobs — OVH Kepler API key empty in config.yaml |
| `oc_checkpoint_store_git_corrupted-20260617` | 2 | checkpoints/store/.git missing refs/heads/ and objects/ |
| `skill_library_stubs` | 4 | 17 stub dirs without SKILL.md |
| `skill_hygiene_followup_20260601` | 2 | 21 stub dirs + 35 nested .git repos |

## Infrastructure

- Gateway: running, PID 325777, 47h uptime, systemd=inactive (expected)
- Disk: 68% (31 GB free / 96 GB)
- state.db: 5.9 GB
- No dead script refs, no dead skill refs, no duplicate jobs