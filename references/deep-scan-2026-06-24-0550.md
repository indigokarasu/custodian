# Deep Scan 2026-06-24 05:50 — Evidence Record

**Verdict:** Clean (all error jobs transient) + 1 Tier 3 escalation

## Gateway
- Restarted at 04:06 via `hermes-update` (update log confirms)
- Process uptime 01:47 at scan time
- `systemctl inactive` + health endpoint unreachable = `--replace` takeover (normal)
- No new errors in errors.log after restart

## Cron Registry
- 133 total jobs, all `status=None` (post-restart pending — normal)
- 0 jobs with active failures (consecutive_failures >= 1 + active error)
- 12 jobs with transient errors (all cf=None/0):
  - 6 monitor scripts + dispatch:briefing-deliver: `exit 1` on no-op (oc_cron_no_agent_exit_1_noop)
  - hermes-dojo-auto, vesper:morning, scout:sources-refresh: HTTP 429 (transient)
  - haiku:follow-maintenance, scout:research: futures shutdown (transient)
  - weave-enrichment-health-check: cf=1, last_status=ok, last_error=null (stale counter)

## Disk
- 88% used (13GB free) — elevated, not critical
- state.db: 8.7GB (symlinked to profile path)

## Escalation
- `oc_config_empty_section_fixloop_20260624` — 4th+ occurrence
  - **Main config** (`<hermes-root>/config.yaml`): max_concurrent_sessions:null (line 11), context_file_max_chars:null (line 89), max_in_progress_per_profile:null (line 125), max_parallel_jobs:null (line 495)
  - **Profile config** (`<hermes-home>/config.yaml`): CLEAN (previous fix on 06-23 held)
  - Root cause: gateway config migration regenerates null keys from template, OR prior fixes only addressed profile config while main config was missed
  - **Lesson**: Must check BOTH config files when investigating this fix-loop

## Clean Verdict Logic Applied
- All 12 error jobs classified as transient (cf=None/0, known patterns)
- Skipped Steps 3b, 4, 5, 9 (per clean verdict shortcut)
- Did NOT return [SILENT] because Tier 3 escalation was needed
- Journal written with `not_activity_reason` + `escalation_needed: true`
