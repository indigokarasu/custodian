# Deep Scan Evidence — 2026-05-30 21:00 UTC

## Confirmed Patterns

### Backtick name mismatch (conformance false positive)
SKILL.md Background tasks tables wrap names in backticks (`` `bower:scan` ``). Cron stores without backticks (`bower:scan`). Stripping backticks before comparison resolved ALL false "missing from cron" results — all 35+ declared tasks matched existing cron jobs.

### Gateway systemd --replace takeover confirmed
`systemctl is-active hermes-gateway` = "inactive" but `curl localhost:8080/health` = "ok". Consistent pattern. Not an error.

### elephas:ingest failure pattern
Failed at 13:50 with no corresponding error in errors.log. Wrapper script (`elephas_ingest_wrapper.sh`) stops ladybug_bridge → runs pipeline → restarts bridge. The non-zero exit likely comes from the pipeline script itself, not logged to errors.log. Monitor for recurrence — if frequent, check `/tmp/ladybug_bridge_elephas.log`.

### skill_view "" empty-string lookups
mentor:light triggers `Skill '' not found` errors. Non-fatal. The job runs successfully despite these warnings.

## Session Stats
- Total cron jobs: 104
- Jobs with error_status today: 4 (vesper:update, email:check, elephas:ingest, lucid:dream)
- All verified as transient or stale
- Tier 1 fixes applied: 0
- Dead skill refs: 0 | Dead script refs: 0 | Duplicates: 0 | Orphans: 0
- Skill library: 19 stub dirs, 42 nested .git, 5 orphan files

## Notable: jobs never ran (valid)
7 jobs have `last_status: null` and `last_run_at: null` but future `next_run_at`:
- scout:sources-refresh (Sunday), forge:skill-audit (Monday), genie:disk-cleanup+weekly-cleanup (Sunday)
- ocas-autobio-update (Sunday), ocas-autobio-distill (monthly), rainbow-grocery-receipts (weekly)
All are declared, valid, and simply waiting for their first scheduled window.
