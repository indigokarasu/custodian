# Light Scan 2026-06-29 22:06 PDT

## Verdict: Clean — all errors transient or already mitigated

**Scan gap:** 8 days since previous journal (light-scan-2026-06-21-201000). Triggered `oc_journal_gap` — see summary for resolution.

## Results

| Job | Error | cf | Classification | Action |
|-----|-------|----|----------------|--------|
| `haiku:follow-maintenance` | HTTP 400 Provider error | None | `oc_cron_provider_error_transient` | None — self-resolves |
| `bower:scan` | HTTP 429 rate limit | None | `oc_cron_429_transient` | None — self-resolves |
| `sands:conflict-scan` | HTTP 400 Provider error | None | `oc_cron_provider_error_transient` | None — self-resolves |
| `email:check` | exit 1 (OAuth invalid_grant) | None | `oc_cron_disabled_stale_error` | None — already paused |
| `monitor:list` | exit 1 (OAuth subprocess) | None | `oc_cron_disabled_stale_error` | None — already paused |

## Key Observations

### 1. Paused jobs with `last_status=error` persist indefinitely
Both `email:check` and `monitor:list` are paused (`enabled: false`, `state: paused`) with root cause `oc_google_oauth_token_revoked`. Their `last_run_at` is AFTER `paused_at`:
- `email:check`: paused 10:34 PDT, last_run 10:35 PDT
- `monitor:list`: paused 18:33 UTC (11:33 PDT), last_run 11:33 PDT

The scheduler ran them one final time before the pause took effect. The error is now frozen — these jobs will never run again to overwrite `last_status`. **Filter paused jobs from errors at scan start.**

### 2. Three new transient errors all first-occurrence (cf=None)
All three null-provider jobs hit generic "Provider returned error" or 429 on their first occurrence. No recurrence, no pattern match to known issue fingerprints. Self-resolving.

### 3. Journal gap (8 days)
No journal between 2026-06-21 and 2026-06-29. Cause: likely the heartbeat cron wasn't triggering light scans for 8 days, or the scans completed without journal output. Not a silent failure — the cron was running (visible in gateway log restarts). The journal gap itself is noted in the delta block.

## Issue Check

- `oc_google_oauth_email_check_invalid_grant_20260628`: OPEN in issues.jsonl ✓ (tracked, jobs paused)
- `oc_fallback_model_manifest_401_20260620`: OPEN in issues.jsonl ✓ (monitoring)
- `skill_library_stubs`: OPEN in issues.jsonl ✓ (user-gated)
- No journal-to-issues gaps found.

## Delta from Previous Scan (8 days)

```
elapsed_days: 8
new_issues: 0
new_errors: 3 (all transient, cf=None)
new_error_detail: haiku:follow-maintenance, bower:scan, sands:conflict-scan — all first-occurrence
stable_issues: oc_google_oauth_token_revoked (paused), oc_fallback_model_manifest_401 (monitoring)
```
