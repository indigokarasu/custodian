# Deep Scan — 2026-06-28 21:05

## Result: Clean Verdict

3 error jobs, 0 fixes applied, 0 new escalations.

## Error Classification

| Job | Fingerprint | Classification | CF | Reason |
|-----|-------------|----------------|----|--------|
| `haiku:morning-scan` | `oc_cron_provider_error_transient` | transient | null | Null-provider job hit first-occurrence `RuntimeError: Provider returned error`. Self-resolves. |
| `monitor:list` | `oc_cron_no_agent_exit_1_noop` | by-design | null | no_agent monitor script. Exit 1 when no new tasks. |
| `email:check` | `oc_google_oauth_token_revoked` | escalated (Tier 3) | null | `invalid_grant: Token has been expired or revoked`. Already escalated as `oc_google_oauth_email_check_invalid_grant_20260628`. |

## Key Decisions

1. **`email:check` classified as `oc_google_oauth_token_revoked` not `oc_cron_no_agent_exit_1_noop`**: The script is `no_agent: true` and exits with code 1, but the actual error is `google.auth.exceptions.RefreshError: invalid_grant` — a real Google OAuth failure, not a no-op exit. The subprocess failure propagates as exit 1 but is NOT by-design. See `references/email-check-invalid-grant-diagnostic.md` for the full diagnostic procedure.

2. **Already-escalated issue not re-escalated**: The Tier 3 issue `oc_google_oauth_email_check_invalid_grant_20260628` was created earlier today with `escalation_needed: true`. Deep scan correctly noted it in the journal but did not create a duplicate escalation.

3. **Config clean**: Both main and profile config.yaml have zero null keys. The `oc_config_empty_section` fix-loop is dormant (no null keys this cycle).

4. **SOUL.md truncation**: Resolved. Zero occurrences in errors.log. Config set to 12000, gateway restarted 2026-06-27.

## Journal

Written to `commons/journals/ocas-custodian/2026-06-28/deep-scan-20260628T210500Z.json`.
