# Light Scan 2026-06-27 18:05

**Run ID**: 20260628T010534Z
**Result**: Clean verdict — [SILENT]

## Summary

5 error jobs found, 0 fixes applied. All classified as transient or stale.

## Error Jobs

| Job | Fingerprint | CF | Classification | Reason |
|-----|-------------|----|----------------|--------|
| haiku:morning-scan | oc_cron_provider_error_transient | None | Transient | Null-provider, first occurrence. Per cron-provider-error-transient-pattern. |
| taste:scan | oc_cron_provider_error_transient | None | Transient | Same pattern. |
| Backup Hermes Sessions to GitHub | oc_cron_stale_error_state | None | Stale | Output file at 18:02 confirms successful GitHub push. jobs.json lags. |
| brief:email-morning | oc_cron_disabled_stale_error | None | Stale | `enabled: false`, disabled by finch:work. Pre-disable error retained. |
| brief:email-evening | oc_cron_disabled_stale_error | None | Stale | `enabled: false`, same as morning. |

## New Patterns Identified

### oc_cron_disabled_stale_error

Disabled/paused jobs retain `last_status=error` from before they were disabled. These are non-actionable — the job is intentionally not running. Should be filtered from actionable error counts.

First seen: `brief:email-morning` and `brief:email-evening`, both disabled by `finch:work` with `paused_reason: "Disabled by finch:work — redundant with sands:*-brief + dispatch:briefing-deliver pipeline."`

### Output-file trumps jobs.json for stale verification

The cron output file at `{profile}/cron/output/{job_id}/{timestamp}.md` is ground truth for the most recent execution. `jobs.json` `last_status` may lag during scheduler state-update. Used this to confirm `Backup Hermes Sessions to GitHub` succeeded at 18:02 despite `last_status=error`.

## Skill Conformance

- 141 total jobs scanned
- 81 jobs with skill field
- 2 missing skill dirs: `ocas-lucid` (archived to `.archive/`)
- ocas-lucid: journals exist through 2026-06-20, non-critical

## Journal

Written to: `<hermes-home>/commons/journals/ocas-custodian/2026-06-27/20260628T010534Z.json`
