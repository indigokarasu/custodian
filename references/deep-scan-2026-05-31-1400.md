# Deep Scan Evidence — 2026-05-31 14:00 UTC

## Tier 1 Fixes Applied

### 1. finch:work task-list.json path mismatch
- **Job**: `finch:work` (id: 9a0b2b502470)
- **Wrong path**: `<hermes-home>/commons/finch/task-list.json`
- **Correct path**: `<hermes-home>/commons/data/ocas-finch/task-list.json`
- **Symptom**: finch:work reported "no tasks" every run; finch:scan fine (uses data/ path)
- **Fix**: Updated job prompt in jobs.json, replaced wrong path
- **Verified**: task-list.json confirmed existing at correct path (20KB)
- **Related**: light-scan-2026-05-30.md noted "finch task-list missing" — this was the root cause

### 2. Stale git lock file
- **File**: `<hermes-home>/checkpoints/store/indexes/e9671acd244849c5.lock`
- **Created**: 2026-05-25 (6 days stale)
- **Effect**: `git add -A` in checkpoint_manager fails with "Another git process seems to be running"
- **Fix**: `rm -f` the 0-byte lock file

## New Patterns
1. **finch:work path convention**: finch jobs may reference `commons/finch/` (wrong) vs `commons/data/ocas-finch/` (correct).
2. **checkpoint git lock files**: Stale lock files in `checkpoints/store/indexes/` can persist for weeks.
3. **MCP simultaneous failure**: When mempalace AND stealth-browser both fail with identical errors, it's systemic.