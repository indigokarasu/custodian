# Deep Scan 2026-06-20 02:00 PT

## System Health
- **Gateway**: Running (PID 797421, 21h59m uptime). systemd=inactive (known --replace pattern). Port 9119 listening.
- **state.db**: 7.0 GB, 0.0% freelist — healthy, no VACUUM needed.
- **Disk**: 75% (72G/96G) — acceptable.
- **Config**: Removed `max_concurrent_sessions: null` from profile config.yaml (Tier 1 fix applied).

## Error Jobs (13 total, all transient cf=None/0)
| Job | Error | Classification |
|-----|-------|---------------|
| forge:update | HTTP 429 | Transient rate limit |
| taste:scan | HTTP 429 | Transient rate limit |
| weave:sync-google | concurrent.futures shutdown | Transient, pause/resume available |
| rainbow-grocery-receipts | 401 auth | First occurrence, monitor |
| praxis:decay_check | 401 auth | First occurrence, monitor |
| dispatch:briefing-deliver | exit 1 no-op | Expected (cf=0) |
| monitor:email | exit 1 no-op | Expected pattern |
| monitor:journals | exit 1 no-op | Expected pattern |
| monitor:koda-issues | exit 1 no-op | Expected pattern |
| monitor:list | exit 1 no-op | Expected pattern |
| monitor:spot | exit 1 no-op | Expected pattern |
| monitor:styx | exit 1 no-op | Expected pattern |
| monitor:taste | exit 1 no-op | Expected pattern |

## Tier 2 Issues
1. **Missing declared tasks**: email:check, email:draft, mentor:light, praxis:journal_ingest declared in SKILL.md but no cron jobs. Cannot auto-register without schedule/prompt details.
2. **401 auth failures**: rainbow-grocery-receipts + praxis:decay_check, first occurrence, monitoring.
3. **Monitor no-op exits**: 8 monitor/dispatch jobs exit 1 when no work — expected but noisy.

## Context Engine
- 200+ "Context engine 'chronicle' not found" warnings — expected noise (config uses engine:compressor, plugin loads from profile path).

## Fixes Applied
- `fix-deep-20260620-0200-config-empty`: Removed `max_concurrent_sessions: null` from profile config.yaml.

## Verdict
System healthy. 1 Tier 1 fix applied. 3 Tier 2 issues surfaced. No Tier 3 escalations.