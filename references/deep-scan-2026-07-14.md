# Deep Scan Evidence — 2026-07-14 09:16 UTC

## Pitfalls exercised (2026-07-14)
- **read_file backend flakiness**: OpenAI-compatible API returned `DaemonThreadPoolExecutor object has no attribute '_initializer'` on first 3 read_file calls; fell back to `terminal(cat)` — reliable.
- **state.db symlink artifact**: `ls -la <hermes-root>/state.db` showed size 0 (it's a symlink). Real size via `du -h state.db` = 14GB. Never trust `ls` size on a symlinked db.
- **No-agent exit-1 de-aggregation**: rally:daily-activity-check + custodian:cron-health both stored `Script exited with code 1` with WARNING stdout. Ran each live → EXIT=0 (watchdogs clean). Both STALE, not active. (gotcha: no_agent silent-mask + seek-by-run_id).
- **False-close detection (Step 8e inverse)**: oc_script_timeout_chronicle_embed_20260713 had been "resolved" 2026-07-13 from a drained-queue re-run. Live re-check: chronicle:daily-embed last_run 2026-07-13T03:12, backlog events=46118 → REOPENED.
- **False-open detection**: oc_google_tasks_access_token_missing_20260714 was created by a prior scan from a masked monitor:list KeyError. monitor:list now status=ok, no access_token error → RESOLVED.
- **oc_state_db_oversized recurrence trap**: disk 82%, state.db 14GB, state-snapshots/ EMPTY (prior "fix" deleted snapshots — transient relief only). DB regrew in ~7d. Snapshot deletion ≠ root-cause prune.
- **Config null-key false positive avoided**: grep `: ($|null$)` matched intentional empty headers (ivp:, soul:, x50:, api_key:) — NOT the `key: null` defect. Verified real values present.

## Fix counts
- Tier1 auto-fixes applied: 0 (registry/config/skill-init all clean).
- issues.jsonl reconciliations: 2 (1 reopen, 1 resolve).
- Escalations raised: 8 open user-gated + 1 reopened code-defect (via journal escalation_payload, escalation_needed=true).

## Script/probe results
- classify_error_jobs.py: 15 enabled-error jobs; 2 no_agent exit1 (de-aggregated, both stale).
- reopen_false_resolutions.py: "No false resolutions found" (provider issues correctly still open).
- classify_llm_necessity_integration.py: 0 llm_unnecessary candidates.
- chronicle_embed_backlog_probe.py: events unembedded=46118 (capped 500/run) → real backlog confirming timeout is live.

## Model note
Active model tencent/hy3:free hit infrastructure errors on tool-result processing; terminal-based tooling used throughout.
