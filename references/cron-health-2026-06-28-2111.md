# Cron Health Check — 2026-06-28 21:11 UTC

## Summary

- **Total jobs:** 139
- **Healthy:** 136
- **last_status=error:** 3
- **consecutive_failures > 0:** 1 (stale counter)
- **Disabled:** 2 (lucid:dream, lucid:update)
- **Previously wasteful cron 7b28d11bdfb3:** Removed (resolved)

## Error Jobs

| Job | ID | Last Run | Error | Classification |
|-----|----|----------|-------|----------------|
| email:check | 25c06979ccc7 | 2026-06-28 14:07 PT | Google OAuth RefreshError | Tier 3 — `oc_google_oauth_token_revoked` (already escalated) |
| monitor:list | 39b7edc44b35 | 2026-06-28 14:07 PT | Script exit code 1 | Subprocess failure (Google OAuth cascade) — NOT exit-1-by-design |
| haiku:morning-scan | c05b5bf553ce | 2026-06-27 15:06 PT | RuntimeError: Provider returned error | Transient — 1 day stale |

## Stale Counter Anomaly

- **weave-enrichment-health-check** (7fd9801171ad): `consecutive_failures=1` but `last_status=ok`, `last_error=null`. Counter stale from a prior transient failure. Next successful run will reset. No fix needed.

## Memory Guard Floor

| Check | Result | Status |
|-------|--------|--------|
| finch:memory-guard-floor recency | Last ran 2h ago (12:05 PT) | ✅ OK |
| decisions.jsonl over_cap_after=true | 2 records, both from 2026-06-16 (12 days ago) | ⚠️ Historical — resolved externally |
| MEMORY.md size | 2,170 / 2,200 chars (1.4% headroom) | ⚠️ WARNING — very tight |

## Technique Notes

1. **Output directory scan** — grep for error markers in `<hermes-home>/cron/output/{job_id}/` latest files. High counts may be false positives (skill pitfall text, not actual errors). Verify by reading the file.
2. **Google OAuth cascade** — `email:check` RefreshError cascades to `monitor:list` (which uses `tasks_monitor.py` calling Google APIs). Both fail from the same revoked token.
3. **monitor:list is NOT exit-1-by-design** — The `oc_cron_no_agent_exit_1_noop` misclassification trap: `monitor:list` wraps `tasks_monitor.py` which calls Google APIs. Exit 1 indicates the subprocess failed (OAuth), not that there's no work. Must distinguish from genuine no-op scripts.
4. **jobs.json structure** — It's `{"jobs": [...], "updated_at": "..."}`, NOT a bare list. Always `data.get("jobs", [])`.