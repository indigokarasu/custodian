# Light Scan Evidence — 2026-06-17 20:00 PDT

## Checkpoint Store Git Corruption — Self-Resolved Pattern

**Observation**: The `oc_checkpoint_store_git_corrupted` issue (95 errors on 2026-06-17) appears to have self-resolved. At 20:00, the `.git` directory exists at `<hermes-home>/checkpoints/store/.git/` but contains only top-level files (HEAD, config, description, hooks/, info/) — no `refs/heads/` or `objects/`. Git reports `fatal: not a git repository`. Yet **0 checkpoint manager errors** appeared in the gateway log today.

**Hypothesis**: The checkpoint manager either (a) gracefully handles the missing `.git` and skips git operations, or (b) the `.git` was recreated by a process that doesn't fully initialize it but also doesn't trigger git commands.

**Diagnostic**: If this pattern recurs — `.git` directory exists but is non-functional AND no errors in logs — classify as **resolved/noise**. Only escalate if error count > 0.

**21:12 follow-up**: Confirmed — error count unchanged at 94, last error at 18:22. No new errors in 3+ hours. This is the **second consecutive occurrence** of this pattern self-resolving without intervention. The `.git` directory remains non-functional but the checkpoint manager has stopped attempting git operations. **Recommendation**: On next occurrence, do not escalate unless error count increases. Classify as `oc_checkpoint_store_git_corrupted_self_resolved` (Tier 2, monitor only).

## Cron Registry Health

- 116/116 jobs scheduled, 0 error status
- Stale counters: elephas:ingest (cf=2), weave-enrichment-health-check (cf=1) — both `last_status=ok`, `last_error=null` — known non-fatal pattern
- No overdue jobs
- No new script path blocks or `Path.home()` issues

## Telegram Errors

- 113 flood control warnings — known transient, self-resolving
- 1 "Message thread not found" error at 14:27 — known non-fatal
- 3 "Overflow split" errors at 19:46 — flood control on long responses, self-resolving

## Skill Hygiene (Tracking)

- 13 stub directories without SKILL.md (down from 21 — user cleanup in progress)
- 30 nested .git repos (down from 35)
- No auto-remediation per policy (requires user confirmation)

## Disk

- 67% usage (33GB free / 96GB total) — healthy, no change
