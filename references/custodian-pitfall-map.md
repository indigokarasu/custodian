| `references/chronicle-embed-forward-stale-resolution-20260714.md` | **The other side of the drained-backlog trap** — when a reopened timeout issue's governed jobs are `paused + ok + empty last_error` AND its own `verified`=true, it IS legitimately forward-stale (the fix held, not a drained queue). Contrast table + re-verify-on-resume trigger so a future scan neither wrongly re-opens it nor leaves it open. |
| `references/chronicle-db-malformed-stale-premise-verify-2026-07-15.md` | Re-verifying `oc_chronicle_db_malformed` / `oc_state_db_oversized` before leaving them escalated: symlink size-0 trap (`du -h` not `ls`), BOUNDED `PRAGMA integrity_check(N)` under a `signal` alarm (unbounded check times out at 60s), live disk% re-derivation. Resolve as stale-premise when healthy. |
| `references/chronicle-enrich-false-backlog-2026-07-15.md` | **Phantom backlog trap**: `chronicle_embed_backlog_probe.py` reports large "unembedded events" counts that `enrich_embeddings.py` skips forever (0-char-text records, `len(text.strip())>=3` guard) — NOT real volume. Plus sibling-script false-reopen correction (a prior scan reopened `oc_script_timeout_chronicle_embed` on a false premise; live re-run 42.5s exit 0 proved healthy). Read before reopening a chronicle timeout issue. |
