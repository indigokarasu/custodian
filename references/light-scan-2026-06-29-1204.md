# Light Scan 2026-06-29 12:04 UTC — Journal-Escalation Gap + MCP Module Persistence

## What happened

This scan (12:04 UTC) found the same 2 OAuth errors as prior scans (delta from 10:05) plus
a **workflow gap**: the 10:05 scan had flagged `oc_mcp_server_module_deleted` with
`escalation_needed: true` in its journal but **never wrote the issue to any issues.jsonl
file**. The 12:04 scan discovered the gap by checking all issues.jsonl paths and finding no
matching entry, then wrote the issue manually.

## Error breakdown

| Job | cf | Root cause | Status |
|-----|-----|------------|--------|
| `email:check` | None | Google OAuth `invalid_grant` (token revoked) | Already escalated, user-gated |
| `monitor:list` | None | Google OAuth `invalid_grant` (subprocess cascade via tasks_monitor.py) | Already escalated, user-gated |

## New escalation written

- **Issue**: `oc_mcp_server_module_deleted_workspace_mcp_20260629`
- **Fingerprint**: `oc_mcp_server_module_deleted`
- **Detail**: `workspace-mcp-fixed` binary at `/usr/local/bin/` exists (51 bytes, Jun 5) but
  `main` Python module is deleted from `<hermes-venv>`. Confirmed via
  `importlib.util.find_spec('main')` returning None. 12+ failed connections since
  Jun 28 22:50. Written to `<hermes-home>/profiles/indigo/commons/data/ocas-custodian/issues.jsonl`.

## Gap uncovered

**Prior scan flagged-but-did-not-persist.** The 10:05 journal has `escalation_needed: true`
and `escalation_reason` describing the MCP module deletion, but no `issues.jsonl` entry was
created for it. This means:
1. The escalation runner would NOT find it (it reads issues.jsonl, not journals).
2. Mentor heartbeat would NOT find it (it reads `escalation_needed: true` tagged journals,
   but the issue.jsonl is the canonical record for escalation runner dedup).
3. Only a later scan checking the gap would catch it.

**Fix**: Added Step 8b to the light-scan checklist — for any prior journal entry with
`escalation_needed: true`, verify a matching issues.jsonl entry exists; if not, write it.

## MCP module diagnosis procedure

```python
import importlib.util
spec = importlib.util.find_spec('main')
# None = module deleted (confirms oc_mcp_server_module_deleted)
```

```bash
ls -la /usr/local/bin/workspace-mcp-fixed  # If EXISTS but import fails = module deleted
```

Distinct from `oc_workspace_mcp_binary_missing` (binary itself absent → "No such file or directory").

## Delta chain

- 09:04 → delta from 08:02 (63 min): same OAuth issues + new transient praxis:journal_ingest 400
- 10:05 → found MCP module issue, flagged in journal, did NOT write to issues.jsonl
- 12:04 → delta from 10:05 (117 min): same OAuth issues, 0 new errors, wrote missing MCP issue

## Gateway events

- Clean restart at 04:02 PDT (SIGTERM → systemd revive). Not an error.
- No new errors in errors.log after restart beyond the known OAuth pattern.
